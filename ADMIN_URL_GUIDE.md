# 管理後台網址指南

## 📋 後台網址說明

### 預設網址格式

根據 `render.yaml` 中的設定，管理後台服務名稱是 `mojan-admin`，所以後台網址應該是：

```
https://mojan-admin.onrender.com
```

### 實際網址可能不同

如果服務名稱有後綴（例如：`mojan-admin-Okuv`），實際網址可能是：

```
https://mojan-admin-okuv.onrender.com
```

## 🔍 如何找到實際的後台網址

### 方法 1：在 Render Dashboard 中查看

1. **進入 Render Dashboard**：
   - 前往 `https://dashboard.render.com`

2. **找到 mojan-admin 服務**：
   - 在服務列表中，找到 `mojan-admin` 或 `mojan-admin-Okuv` 服務

3. **查看服務 URL**：
   - 點擊服務進入詳情頁
   - 在服務頁面頂部，會顯示服務的 URL
   - 格式：`https://[service-name].onrender.com`

4. **複製網址**：
   - 點擊 URL 旁邊的複製圖示
   - 或直接複製顯示的 URL

### 方法 2：檢查服務狀態

1. **進入服務詳情頁**：
   - 點擊 `mojan-admin` 服務

2. **查看服務資訊**：
   - 在服務詳情頁中，URL 會顯示在頂部
   - 確認服務狀態為 "Deployed"

3. **測試網址**：
   - 訪問 `https://[service-name].onrender.com/admin/login`
   - 應該會顯示登入頁面

## 📝 後台網址結構

### 主要頁面

1. **登入頁面**：
   ```
   https://mojan-admin.onrender.com/admin/login
   ```

2. **管理面板首頁**（登入後）：
   ```
   https://mojan-admin.onrender.com/admin
   ```

3. **其他管理頁面**：
   - 用戶管理：`/admin/user-management`
   - 俱樂部管理：`/admin/club-management`
   - 遊戲記錄管理：`/admin/game-record-management`
   - 支付管理：`/admin/payment-management`
   - 代理管理：`/admin/agent-management`

## ⚠️ 重要提醒

### 1. 免費方案限制

- Render 免費方案在 15 分鐘無活動後會休眠
- 首次訪問可能需要較長時間喚醒服務（約 30-60 秒）
- 這是正常現象，請耐心等待

### 2. 登入資訊

- **用戶名**：使用您在環境變數中設定的 `ADMIN_USERNAME`
- **密碼**：使用您在環境變數中設定的 `ADMIN_PASSWORD`

### 3. HTTPS

- Render 自動提供 HTTPS
- 所有網址都使用 `https://` 開頭

## ✅ 驗證步驟

1. **確認服務狀態**：
   - 在 Render Dashboard 中，確認 `mojan-admin` 服務狀態為 "Deployed"

2. **訪問登入頁面**：
   - 訪問 `https://mojan-admin.onrender.com/admin/login`
   - 應該會顯示登入頁面

3. **測試登入**：
   - 使用設定的 `ADMIN_USERNAME` 和 `ADMIN_PASSWORD` 登入
   - 確認可以成功登入管理面板

## 🔗 相關網址

### 其他服務網址

1. **遊戲伺服器**：
   ```
   https://mojan-server.onrender.com
   ```
   - 健康檢查：`https://mojan-server.onrender.com/health`

2. **前端應用**：
   ```
   https://mojan-app.onrender.com
   ```

3. **資料庫**：
   - 只能通過 Internal Database URL 連接
   - 不提供公開訪問

---

最後更新：2024年11月

