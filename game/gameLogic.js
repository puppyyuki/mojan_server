/**
 * 遊戲核心邏輯
 * 包含所有遊戲相關的函數
 * 
 * 注意：由於遊戲邏輯非常龐大且複雜，大部分函數暫時保留在 server.js 中
 * 這個文件作為接口，導出需要在其他地方使用的函數
 * 
 * 未來可以逐步將函數遷移到這個文件中
 */

// 這些函數需要從 server.js 中導出
// 暫時使用占位符，實際實現仍在 server.js 中

module.exports = {
  // 這些函數會在 server.js 中定義並導出
  // 例如：
  // drawTile: require('./server').drawTile,
  // discardTile: require('./server').discardTile,
  // 等等...
  
  // 暫時導出空對象，實際函數在 server.js 中
};

